<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.audiro.repository.FavoriteDestinationDao">
	
	<!-- 찜한 여행지 불러오기 -->
	<select id="selectOrderById" resultType="FavoriteDestination">
		select * from favorite_destination f
		join travel_destination t
		on f.favorite_destination_id = t.travel_destination_id
		order by favorite_destination_id
	</select>
	
</mapper>